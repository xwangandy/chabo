# 插播网页端执行计划

版本：v0.7
日期：2026-05-02
状态：执行中

## 1. 执行原则

本项目按“复杂交互最优”推进，不按“最省事后台”推进。框架已经定版：

```text
React + TypeScript + Vite
React Router
TanStack Query
Ant Design
AG Grid Enterprise
FastAPI JSON API
```

执行期间默认自主推进，不逐项等待确认。只有以下情况需要暂停说明：

- 需要真实 Telegram Bot token、域名、服务器、支付或公司 SSO 凭证。
- 需要决定商业授权，例如 AG Grid Enterprise 是否购买。
- 业务规则存在互相冲突，无法通过保守默认值继续实现。

## 2. 总阶段

### M0：工程骨架

- 新建 `web/` React/Vite 前端工程。
- 新建 `src/chabo/webapi/` FastAPI JSON API。
- 新增 `portal_access`、`web_sessions`、`login_tokens`、`placement_plans` 等基础表。
- 增加 `chabo run-api`。
- 管理端、广告主端、频道主端先有可登录、可路由、可读取指标的页面。

验收：

- `python3 -m unittest discover -s tests` 通过。
- `python3 -m compileall -q src tests` 通过。
- `npm run build` 通过。

### M1：认证与权限闭环

- Telegram Mini App `initData` 校验。
- Bot 一次性 magic link 登录。
- 管理员账号登录。
- portal access 自动开通和手工授予。
- 管理员 impersonation。

验收：

- 无权限访问返回 403。
- 管理员可切换三端门户。
- 广告主/频道主只能访问自己数据。

### M2：管理端 MVP

- 订单列表、订单详情、审核、拒绝。
- 投放列表、失败投放、退款。
- 充值申请审核。
- 争议处理。
- 账户、频道检索。
- 审计日志。

验收：

- 管理员可不用 Bot 完成核心运营动作。
- 所有高影响动作写 audit/tool call log。

### M3：广告主端 MVP

- 广告素材库。
- 频道市场。
- 收藏/频道集合。
- 投放计划草稿。
- AG Grid 投放矩阵。
- 计划校验和提交生成订单。
- 钱包与订单。

验收：

- 广告主可从网页端完成批量投放计划并生成订单。
- 矩阵支持跨页选择、批量编辑和校验结果。

### M4：频道主端 MVP

- 频道列表。
- 频道详情配置。
- 展示形态开关。
- 价格和每日限制。
- 收益与广告记录。

验收：

- 频道主可管理多个频道并查看收益。

### M5：H5 适配

- 广告主 H5 简化下单。
- 频道主 H5 基础管理。
- 管理端 H5 审核和异常处理。

验收：

- 手机端不展示完整复杂矩阵，改用集合、模板和摘要确认。

### M6：上线与稳定性

- Nginx SPA + API 配置。
- systemd 服务样例补齐 `chabo-api`。
- Playwright 冒烟。
- 性能和慢查询检查。
- PostgreSQL 迁移评估。

## 3. 当前执行状态

- M0 第一批已完成：
  - `web/` React + TypeScript + Vite 工程已创建。
  - 前端已接入 React Router、TanStack Query、Ant Design、AG Grid。
  - `src/chabo/webapi/` FastAPI JSON API 已创建。
  - `portal_access`、`web_sessions`、`login_tokens`、`placement_plans` 等基础表已入库。
  - `chabo run-api` 已可启动 API。
  - 三端门户基础页面已可通过开发登录访问。
  - Python 编译、Python 单元测试、前端生产构建均已通过。
- M1 第一批已完成：
  - Telegram Mini App `initData` 签名校验已实现。
  - 一次性 magic link 生成与消费已实现。
  - 前端 `/login/magic` 已可消费登录链接。
  - API 测试覆盖 magic link 单次消费和 Telegram WebApp 登录。
- M2 管理端 MVP 第二批已完成：
  - 管理端已接入待审订单、待审入账列表。
  - 管理端 API 已接入订单审核通过、订单拒绝、入账通过、入账拒绝动作。
  - 管理端已接入投放列表、争议列表、投放退款、频道主删帖异常处理、争议裁决动作。
  - 管理端订单审核/拒绝已写入当前管理员 actor。
- M2 管理端 MVP 第三批已完成：
  - FastAPI 已新增订单详情、投放详情、争议详情接口，返回基础对象、时间线、证据快照和账本流水。
  - React 管理端列表已支持打开详情抽屉。
  - 管理端已新增账户检索、频道检索、审计日志检索。
- M2 管理端 MVP 第四批已完成：
  - 管理端 FastAPI 已支持网页端发起双人复核入账申请。
  - React 管理端已新增入账申请表单，可由一名管理员创建申请，另一名管理员审批。
- M3 广告主端 MVP 第二批已完成：
  - 广告素材库已支持创建、列表、归档。
  - 频道市场已支持 AG Grid 多选频道，并选择素材、投放位置生成计划草稿。
  - 投放计划已支持有效项校验、预算汇总、提交后批量生成真实订单。
  - 广告主订单列表已接入网页端。
- M3 广告主端 MVP 第三批已完成：
  - React 投放计划列表已支持展开查看计划项明细、频道、位置、单价、预计预算和校验错误。
- M4 频道主端 MVP 第二批已完成：
  - 频道列表已支持展开真实频道详情。
  - 频道主可在网页端修改每日广告上限。
  - 频道主可在网页端按展示形态修改开关和价格档位。
- M4 频道主端 MVP 第三批已完成：
  - FastAPI 已新增频道主单频道广告记录接口。
  - React 频道主展开行已展示该频道广告记录、广告主、计划时间、收入和退款信息。
- M4 频道主端 MVP 第四批已完成：
  - 频道主 FastAPI 已支持按频道和展示形态修改具体刊例价。
  - React 频道主配置区已支持直接输入 USD 价格并保存。
- M5 H5 适配第一批已完成：
  - 广告主 H5 频道市场不再展示 AG Grid，改为频道卡片选择、素材/位置选择和生成计划。
  - 管理端 H5 新增待审核订单、待审入账、投放异常、争议四组紧急处理卡片。
  - 频道主 H5 新增频道卡片入口，可展开进入每日上限、刊例价、形态开关和广告记录。
  - 手机端抽屉、搜索区和操作按钮已做宽度约束，避免核心操作被表格挤出屏幕。
- M5 H5 适配第二批已完成：
  - 广告主 H5 生成计划后新增“计划摘要”确认页，展示频道数、有效项、无效项和总预算。
  - 摘要页可直接一键提交计划并生成订单；存在无效频道时显示错误摘要并禁用提交。
  - 广告主 H5 有素材时默认选择第一条素材，减少手机端下单步骤。
  - 频道主 H5 广告记录从桌面表格改为移动端卡片，避免展开频道配置后出现横向撑宽。
  - 关键 H5 区域补充稳定 `data-testid`，便于后续视觉冒烟和端到端测试。
- M6 上线与稳定性第一批已完成：
  - 已新增 `DOC/部署/systemd-chabo-api.service`，提供 `chabo-api` systemd 服务样例。
  - 已新增 `DOC/部署/nginx-chabo-web.conf`，提供 React SPA 静态托管和 `/api/` 反向代理样例。
  - 已新增 `DOC/部署/web-spa-api-deploy.md`，覆盖构建、部署目录、健康检查、回滚和验收步骤。
  - README 已补充网页端部署文档入口。
  - 已用 390px 宽度移动视口对广告主计划摘要、管理端紧急处理、频道主频道配置做视觉冒烟，三端均无横向溢出。
- M6 上线与稳定性第二批已完成：
  - 网页端默认主题已切换为暗黑风格，Ant Design、页面背景、移动卡片、JSON 详情和 AG Grid 同步适配。
  - Web API 新增生产配置开关：`CHABO_WEB_ALLOWED_ORIGINS`、`CHABO_SESSION_COOKIE_SECURE`、`CHABO_SESSION_COOKIE_SAMESITE`、`CHABO_API_PROXY_HEADERS`、`CHABO_API_FORWARDED_ALLOW_IPS`。
  - CORS 不再硬编码 localhost，生产可只允许正式域名；HTTPS 下 session cookie 可启用 Secure。
  - 新增 `web/scripts/h5-smoke.mjs` 与 `npm run smoke:h5`，把 390px H5 冒烟固化为可复跑脚本。
  - 部署说明补充生产安全项、journald 巡检、SQLite 备份策略和 PostgreSQL 迁移触发条件。
  - AG Grid 已补充 v34 模块注册与 legacy CSS 主题配置，避免频道市场运行时报模块/主题错误。
- M6 上线与稳定性第三批已完成：
  - Web API 新增 `CHABO_DEV_AUTH_BYPASS` 本地开发免登录开关，默认关闭；开启后自动创建开发账号并授予管理端、广告主端、频道主端权限。
  - React `/login` 会先检测当前会话；已有真实 session 或开发免登录身份时自动跳转，不再卡在账号密码页。
  - H5 冒烟脚本支持 `CHABO_H5_SMOKE_AUTH_BYPASS=1`，本地开发无需管理 token 也能做视觉检查。
  - `chabo preflight` 新增 `web_security` 检查，生产自检时若开启开发免登录会直接判为 critical。
  - 移动/窄屏侧边栏断点和触发按钮间距已调整，避免展开按钮压住页面标题。
- M6 上线与稳定性第四批已完成：
  - React 启动阶段接入 Ant Design v5 的 React 19 兼容渲染入口，使用 `react-dom/client.createRoot` 处理静态弹层/波纹渲染，消除开发期兼容告警。
  - 新增 `chabo seed-web-demo` CLI，用服务层生成本地/演示库数据：三端权限、演示频道、广告素材、待审订单和待审入账。
  - 演示数据生成遵守现有业务约束，不绕过频道高级功能和审计 actor_kind 校验；重复执行不会无限堆叠演示待审订单。
  - README 和部署说明已补充免登录开发、H5 冒烟和演示数据命令。
- M6 上线与稳定性第五批已完成：
  - 新增 `chabo verify-web` 一键验收命令，串起 Python 编译、Python 单测、前端构建、preflight、API health，并可选执行 H5 冒烟。
  - `verify-web --profile local` 会自动允许本地开发免登录和弱 token 告警，可配合 `--seed-demo` 准备演示数据。
  - `verify-web --profile production` 保持严格 preflight，适合部署前用正式域名和 `/api/health` 做 gate。
  - `preflight` 新增 `--allow-dev-auth-bypass`，只供 local/staging 验收显式放行，生产默认仍会把免登录判为 critical。
  - README、React 部署说明和上线回归清单已更新为优先使用 `chabo verify-web`。
- M6 上线与稳定性最后一批已完成：
  - GitHub Actions 已接入网页端 CI gate：PR / push 会安装前端依赖，并执行 `chabo verify-web --profile local --skip-tests --skip-health` 覆盖前端构建和本地 preflight。
  - GitHub Actions 手动触发时可填写 `production_host`，执行 `chabo verify-web --profile production --host <host> --health-url <url>`，作为正式发布前的远端 health + 构建 gate。
  - 新增 `.env.production.example` 和 `web/.env.production.example`，分别作为 `/etc/chabo/env` 与 Vite 生产构建模板；`.gitignore` 已忽略真实生产 env 文件。
  - 新增 `scripts/deploy-web-production.sh`，生产机可读取 `/etc/chabo/env`，执行依赖安装、production verify、重启 `chabo-api`、重启后二次 production verify。
  - `DOC/部署/systemd-chabo-api.service` 补充 `ExecStartPre=chabo preflight --host "$CHABO_PUBLIC_HOST"`、StateDirectory/LogsDirectory 和更完整的 systemd 收紧项。
  - `DOC/部署/nginx-chabo-web.conf` 补充反代头、超时、静态缓存和基础安全响应头。
  - README、部署说明、上线回归清单和网页端开发文档已更新为最终 M6 部署闭环。
- 本轮验证：
  - `bash -n scripts/deploy-web-production.sh` 通过。
  - GitHub Actions workflow YAML 解析通过。
  - `python3 -m unittest tests.test_webapi` 通过，14 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `python3 -m unittest discover -s tests` 通过，131 tests OK。
  - `npm run build` 通过。
  - `chabo verify-web --profile production --host chabo.example --health-url https://chabo.example/api/health --dry-run` 通过，确认 production preflight 不自动放行弱 token / 开发免登录。
  - `chabo verify-web --profile local --skip-tests --skip-health` 通过，模拟 CI local gate。
  - `/api/health` 返回 `ok=true`，`git diff --check` 通过。
- M7 正式登录与权限收口第一批已完成：
  - Web API 新增 `/api/auth/config`，前端可识别 Telegram 登录可用性、本地开发开关和运行环境。
  - `CHABO_ENV=production` 下运行时禁止开发免登录；`/api/auth/dev-session` 默认关闭，生产或未启用时返回 404。
  - 新增 `CHABO_DEV_SESSION_ENABLED`，只供本地/测试使用；`preflight` 会在 production 环境下把它开启判为 critical。
  - Telegram Mini App 登录会创建候选门户，并在每次 `/api/auth/me` 时按真实业务活动自动刷新 portal 状态。
  - 广告主端：候选广告主首次投放审核通过后自动升为 active。
  - 频道主端：候选频道主名下频道出现真实投放 delivery 后自动升为 active。
  - 普通 `/login` 改为正式 Telegram / magic link 入口，不再暴露管理 Token 表单。
  - 新增隐藏 `/login/admin`，可用管理 Token 生成一次性 magic link 并进入管理端或指定门户。
  - 前端无权限页会识别 candidate 状态并显示待开通说明。
- M7 第一批验证：
  - `python3 -m unittest tests.test_webapi` 通过，18 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `python3 -m unittest discover -s tests` 通过，135 tests OK。
  - `npm run build` 通过。
  - `chabo verify-web --profile local --skip-tests --skip-health` 通过。
  - 模拟 `CHABO_ENV=production` 且误开 `CHABO_DEV_AUTH_BYPASS=1 / CHABO_DEV_SESSION_ENABLED=1`，`chabo preflight` 正确 exit 2。
  - 本地 API 已重启到 M7 逻辑，`/api/auth/config` 和 `/api/auth/me` 返回新字段；浏览器验证 `/admin` 可正常进入。
- 后续从 M7 第二批继续：接入 Bot 侧“打开网页端”按钮生成 magic link、补管理员分级权限与管理员代用户查看。
- M7 第二批已完成：
  - Bot 广告主工作台、频道主工作台和设置页新增“打开网页端”按钮，基于 `CHABO_PUBLIC_BASE_URL` 生成一次性 magic link。
  - Web API 新增管理员等级：`viewer / operator / finance / super_admin`，敏感写操作按等级强校验。
  - 管理端账户列表展示三端门户状态和管理员等级；`super_admin` 可点击代看广告主端或频道主端。
  - 代看使用独立 `impersonation_sessions` 与带 `impersonator_account_id` 的 Web session，顶部显示“管理员代看中”，可一键返回管理端。
  - `chabo preflight` 已检查生产 `CHABO_PUBLIC_BASE_URL` 必须是 HTTPS，避免 Bot 生成不可用登录链接。
- M7 第二批验证：
  - `python3 -m unittest tests.test_webapi` 通过，20 tests OK。
  - Bot magic link 按钮与原菜单导航定向测试通过。
  - `python3 -m unittest discover -s tests` 通过，138 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `npm run build` 通过。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，390px 三端无横向溢出。
- M7 第三批已完成：
  - Web API 新增 `PUT /api/admin/accounts/{account_id}/portals/{portal}`，由 `super_admin` 手工调整 `admin / advertiser / publisher` 门户状态。
  - 广告主端、频道主端支持手工开通、设候选、撤销；撤销状态会阻止自动开通规则重新激活。
  - 管理员端支持设置 `viewer / operator / finance / super_admin` 等级或撤销管理员；禁止当前超级管理员把自己降级或撤销。
  - 管理端账户列表新增“权限”抽屉，可直接执行门户开通、撤销和管理员等级设置。
  - 所有手工权限调整写入 `portal_access_events` 与 `audit_logs`。
- M7 第三批验证：
  - `python3 -m unittest tests.test_webapi` 通过，21 tests OK。
  - `python3 -m unittest discover -s tests` 通过，139 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `npm run build` 通过。
  - `chabo verify-web --profile local --skip-tests --skip-health` 通过。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，390px 三端无横向溢出。
- M7 第四批已完成：
  - Web API `/api/admin/audit-logs` 新增 `category` 查询参数，支持 `permission / impersonation / admin_level` 三类审计筛选。
  - 管理端审计页新增“全部 / 权限调整 / 代看 / 管理员等级”分段筛选，能配合搜索框定位具体账号或动作。
  - 权限调整审计 payload 已收口：只有管理端等级调整才写入 `admin_level`，避免管理员等级筛选混入普通广告主端/频道主端开关。
  - 新增 `web/scripts/permission-audit-e2e.mjs` 和 `npm run e2e:permissions`，覆盖“开通广告主端 -> 设置管理员等级 -> 代看 -> 返回管理端 -> 撤销权限 -> 审计分类筛选”。
- M7 第四批验证：
  - `python3 -m unittest tests.test_webapi` 通过，21 tests OK。
  - `python3 -m unittest discover -s tests` 通过，139 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `npm run e2e:permissions` 通过。
  - `npm run build` 通过。
  - `chabo verify-web --profile local --skip-tests --skip-health` 通过。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，390px 三端无横向溢出。
- M7 第五批已完成：
  - Web API `/api/admin/audit-logs` 支持 `actor / target / created_from / created_to` 过滤，列表返回目标账号 Telegram 与展示名。
  - 新增 `/api/admin/audit-logs/export.csv`，按同一组筛选条件导出审计 CSV，最多 5000 行。
  - 管理端审计页新增操作者、目标账号、时间范围筛选，支持详情抽屉查看单条 payload，并支持导出 CSV。
  - 手工权限调整 API 不再接受空备注；前端权限抽屉要求先填写备注，并在开通、设候选、撤销和管理员等级调整前弹出二次确认。
  - `npm run e2e:permissions` 已更新为覆盖备注必填与确认弹窗。
- M7 第五批验证：
  - `python3 -m unittest tests.test_webapi` 通过，21 tests OK。
  - `python3 -m unittest discover -s tests` 通过，139 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `npm run build` 通过。
  - `npm run e2e:permissions` 通过。
  - `chabo verify-web --profile local --skip-tests --skip-health` 通过。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，390px 三端无横向溢出。
- M7 第六批已完成：
  - 审计配置新增 `CHABO_AUDIT_RETENTION_DAYS` 与 `CHABO_AUDIT_EXPORT_MAX_ROWS`，生产 `preflight` 会校验保留天数至少 365 天、导出上限大于 0。
  - `/api/admin/audit-logs/export.csv` 改为仅 `super_admin` 可用，并受 `CHABO_AUDIT_EXPORT_MAX_ROWS` 限制。
  - 权限变更审计 payload 新增 `before / after / changed_fields`，可回看门户状态、管理员等级和撤销时间等字段的变化。
  - 管理员代看 API 和前端抽屉均要求填写代看原因；空原因会被后端拒绝。
  - 超级管理员高危权限操作增加确认口令：调整管理员权限或撤销任一门户时必须输入 `确认调整权限`，后端也会校验。
- M7 第六批验证：
  - `python3 -m unittest tests.test_webapi` 通过，21 tests OK。
  - `python3 -m unittest discover -s tests` 通过，139 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `npm run build` 通过。
  - `npm run e2e:permissions` 通过。
- M7 第七批已完成：
  - 新增统一审计写入 helper，所有业务侧新写入的 `audit_logs` 会生成 `previous_hash / audit_hash / hash_version`，形成 SHA-256 链式摘要。
  - `audit_logs` 迁移新增 hash 字段和查询索引；新列索引只在迁移中创建，避免旧库启动时先建索引后加列的顺序问题。
  - 审计列表返回 `chain_head / retention_days`，管理端审计页支持服务端分页并展示当前链头。
  - 审计详情抽屉展示链式 Hash、上一条 Hash、Hash 版本，并把权限调整的 `changed_fields` 渲染成“字段 / 变更前 / 变更后”对比表。
  - 审计 CSV 导出新增 `previous_hash / audit_hash / hash_version` 列，响应头返回 `X-Chabo-Audit-Export-Sha256` 和导出前链头；导出动作自身也写入审计日志。
  - 代看会话使用独立 `CHABO_IMPERSONATION_SESSION_TTL_SECONDS`，前端顶部显示“管理员代看中”到期时间，并把返回按钮改为更明确的“结束代看”。
  - `npm run e2e:permissions` 已扩展为校验审计详情抽屉里的链式 Hash 和字段变更 UI。
- M7 第七批验证：
  - `python3 -m unittest tests.test_webapi` 通过，21 tests OK。
  - `python3 -m unittest discover -s tests` 通过，139 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `npm run build` 通过。
  - `npm run e2e:permissions` 通过。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，390px 三端无横向溢出。
  - `chabo verify-web --profile local --skip-tests --skip-health` 通过。
  - `git diff --check` 通过。
- 后续建议进入 M8：
  - 做审计链校验工具或 API，能从任意时间范围重新计算 hash 并输出完整性报告。
  - 做一次生产部署彩排：备份旧 SQLite、运行迁移、执行 `chabo verify-web --profile production`、回滚演练。
  - 将审计页查询从 SQLite 适配到未来 PostgreSQL 的分页/索引策略，避免数据量上来后页面变慢。
- M8 上线前产品化与安全验收第一批已完成：
  - 新增 `verify_audit_chain` 审计链校验器，能重算 `audit_logs` 的 SHA-256 链式摘要，报告检查行数、签名行数、旧未签名行数、断链、Hash 异常和当前链头。
  - 新增 `GET /api/admin/audit-logs/verify`，仅 `super_admin` 可执行，管理端审计页新增“校验链路”按钮和完整性报告抽屉。
  - 新增 `chabo verify-audit-chain` CLI，篡改或断链时非零退出；`chabo verify-web --audit-chain` 可把审计链校验纳入本地/生产 gate。
  - 生产部署脚本新增发布前 SQLite 备份，并在重启前后两次 `verify-web` 中加入 `--audit-chain`；脚本输出回滚提示。
  - 广告主端钱包页从占位指标升级为真实运营视图，展示冻结订单和最近账本流水。
  - 频道主端收益页新增频道维度收益表，展示待确认、已确认、平台费和广告记录数。
  - 权限 E2E 扩展为点击“校验链路”并校验审计链报告抽屉。
- M8 第一批验证：
  - `python3 -m unittest tests.test_webapi` 通过，23 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `npm run build` 通过。
  - `npm run e2e:permissions` 通过。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，390px 三端无横向溢出。
  - `python3 -m chabo.cli verify-audit-chain` 通过。
  - `python3 -m chabo.cli verify-web --profile local --skip-tests --skip-health --audit-chain` 通过。
  - `python3 -m chabo.cli verify-web --profile production --host chabo.example --health-url https://chabo.example/api/health --audit-chain --dry-run` 通过。
  - `bash -n scripts/deploy-web-production.sh` 通过。
- M8 上线前产品化与安全验收第二批已完成：
  - 管理端审计页的“校验链路”接入当前时间范围过滤，并新增“严格”开关；报告抽屉展示严格模式与校验时间窗，方便上线前按窗口复核。
  - 审计 API 覆盖 `created_from / created_to / strict_unsigned` 组合，`verify-audit-chain` 继续作为 CLI 守门工具。
  - `audit_logs` 新增 `action + created_at` 索引迁移，配合已有 `created_at / actor / entity` 索引支撑大数据量下的分类筛选、时间窗筛选和分页。
  - 新增 `scripts/rollback-web-production.sh`，支持 `--dry-run`，回滚前会先备份当前 SQLite，再停服务、恢复指定备份、启动服务并执行带 `--audit-chain` 的 production gate。
  - 广告主钱包页和频道主收益页补 H5 卡片视图，390px 移动端不再依赖横向表格读取冻结订单、流水和频道收益。
  - 权限 E2E 扩展为开启“严格”后执行审计链校验，并检查报告抽屉显示严格模式。
- M8 第二批验证：
  - `python3 -m unittest tests.test_webapi` 通过，23 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `npm run build` 通过。
  - `npm run e2e:permissions` 通过。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，390px 三端无横向溢出。
  - `python3 -m unittest discover -s tests` 通过，141 tests OK。
  - `python3 -m chabo.cli verify-audit-chain --created-from "2026-05-02 11:00:00" --strict` 通过。
  - `python3 -m chabo.cli verify-web --profile local --skip-tests --skip-health --audit-chain` 通过。
  - `python3 -m chabo.cli verify-web --profile production --host chabo.example --health-url https://chabo.example/api/health --audit-chain --dry-run` 通过。
  - `scripts/rollback-web-production.sh --dry-run` 通过，确认备份当前 DB、停服、恢复、启动和 production gate 顺序。
  - `bash -n scripts/deploy-web-production.sh scripts/rollback-web-production.sh` 通过。
  - `git diff --check` 通过。
- M8 上线前产品化与安全验收第三批已完成：
  - 管理端新增 `/api/admin/wallet` 与 `/api/admin/settings`，分别提供全站资金总览、最近账本、资金账户榜，以及生产安全开关、审计策略、数据库备份和发布准入状态。
  - 管理端导航新增“钱包总览”和“上线设置”；桌面端以独立 tab 展示，H5 紧急处理页也加入钱包/上线设置卡片。
  - 新增 `scripts/export-audit-integrity-report.sh`，可按时间窗和严格模式导出审计链完整性 JSON，并生成 `.sha256` 留档。
  - 新增 `scripts/rehearse-sqlite-restore.sh`，把当前库和备份库复制到临时目录，在临时恢复库上跑迁移、审计链校验和 preflight。
  - 新增最终生产 Runbook：`DOC/部署/production-runbook.md`，串起发布前 gate、审计留档、备份恢复演练、正式发布、发布后巡检和回滚。
  - 审计保留策略收口为 SQLite 阶段不直接删除 `audit_logs`，优先整库归档 + 审计报告留档；PostgreSQL 阶段再做月分区/冷归档。
- M8 第三批验证：
  - `python3 -m unittest tests.test_webapi` 通过，23 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `python3 -m unittest discover -s tests` 通过，141 tests OK。
  - `npm run build` 通过。
  - `npm run e2e:permissions` 通过。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，390px 三端无横向溢出。
  - `python3 -m chabo.cli verify-audit-chain --created-from "2026-05-02 11:00:00" --strict` 通过。
  - `python3 -m chabo.cli verify-web --profile local --skip-tests --skip-health --audit-chain` 通过。
  - `python3 -m chabo.cli verify-web --profile production --host chabo.example --health-url https://chabo.example/api/health --audit-chain --dry-run` 通过。
  - `scripts/export-audit-integrity-report.sh --created-from "2026-05-02 11:00:00" --strict` 通过，生成 JSON 与 sha256。
  - `scripts/rehearse-sqlite-restore.sh` 通过，临时 SQLite 恢复库通过迁移、审计链和 preflight。
  - `bash -n scripts/deploy-web-production.sh scripts/rollback-web-production.sh scripts/export-audit-integrity-report.sh scripts/rehearse-sqlite-restore.sh` 通过。
- M8 上线前产品化与安全验收第四批已完成：
  - 权限 E2E 新增管理端“钱包 / 设置”入口断言，检查 `/admin#wallet` 的资金指标、最近账本、资金账户榜，以及 `/admin#settings` 的审计策略和发布准入。
  - H5 冒烟新增 `/admin#wallet` 与 `/admin#settings` 两个移动视口检查点，并输出 `admin-wallet-390.png / admin-settings-390.png`。
  - 后端权限测试补齐 viewer / finance / super_admin 边界：viewer 可看普通管理数据但不能建入账、看钱包或导出审计；finance 可看钱包和建入账，但不能导出审计 CSV；错误 detail 明确返回所需等级。
  - 管理端钱包和上线设置面板新增稳定 `data-testid`，减少 E2E 对文案和表格结构的脆弱依赖。
  - 生产 Runbook 补失败处理分支：production gate、审计报告、恢复演练、发布脚本、H5 冒烟、回滚失败时的处置顺序。
- M8 第四批验证：
  - `python3 -m unittest tests.test_webapi` 通过，23 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `python3 -m unittest discover -s tests` 通过，141 tests OK。
  - `npm run build` 通过。
  - `npm run e2e:permissions` 通过，包含钱包/设置入口断言。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，新增 admin-wallet/admin-settings 两个 390px 检查点且无横向溢出。
  - `python3 -m chabo.cli verify-audit-chain --created-from "2026-05-02 11:00:00" --strict` 通过。
  - `python3 -m chabo.cli verify-web --profile local --skip-tests --skip-health --audit-chain` 通过。
  - `python3 -m chabo.cli verify-web --profile production --host chabo.example --health-url https://chabo.example/api/health --audit-chain --dry-run` 通过。
  - `bash -n scripts/deploy-web-production.sh scripts/rollback-web-production.sh scripts/export-audit-integrity-report.sh scripts/rehearse-sqlite-restore.sh` 通过。
  - `git diff --check` 通过。
- M9 上线交付与合并准备第一批已完成：
  - 新增 `DOC/部署/m9-delivery-checklist.md`，整理交付范围、文件归属、敏感配置检查、最终发布检查表、建议提交信息和 PR / 合并说明模板。
  - 确认当前分支为 `main`，与 `origin/main` 同步；远程为 `https://github.com/xwangandy/chabo.git`。
  - 确认 `.gitignore` 已覆盖 `.runtime/`、`web/.runtime/`、`web/dist/`、`node_modules/`、SQLite、日志、pycache 和 egg-info，本地 H5 截图、审计报告演练产物、数据库备份不会进入提交。
  - 确认 `git ls-files --others --exclude-standard` 只剩新增源码、文档、脚本和 env 模板；`chabo.sqlite3`、日志、构建产物和 node_modules 均被忽略。
  - 扫描 env 模板：`.env.example / .env.production.example / web/.env.*` 只包含占位值或本地开发值，未发现真实生产 token；生产真实配置仍要求只写 `/etc/chabo/env`。
  - `README.md` 与生产 Runbook 已链接 M9 交付清单，后续合并或 PR 可以直接复用该模板。
- M9 第一批最终验证：
  - `python3 -m unittest tests.test_webapi` 通过，23 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `python3 -m unittest discover -s tests` 通过，141 tests OK。
  - `npm run build` 通过。
  - `npm run e2e:permissions` 通过。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，包含 advertiser/admin/admin-wallet/admin-settings/publisher 五个 H5 检查点。
  - `python3 -m chabo.cli verify-audit-chain --created-from "2026-05-02 11:00:00" --strict` 通过。
  - `python3 -m chabo.cli verify-web --profile local --skip-tests --skip-health --audit-chain` 通过。
  - `python3 -m chabo.cli verify-web --profile production --host chabo.example --health-url https://chabo.example/api/health --audit-chain --dry-run` 通过。
  - `bash -n scripts/deploy-web-production.sh scripts/rollback-web-production.sh scripts/export-audit-integrity-report.sh scripts/rehearse-sqlite-restore.sh` 通过。
- M9 上线交付与合并准备第二批已完成：
  - 已复核当前 Git 状态：工作分支为 `main`，跟踪 `origin/main`，远程为 `https://github.com/xwangandy/chabo.git`，大批量变更仍未 staged / commit / push。
  - 已确认本地存在 `codex/web-admin-react` 分支且与当前基线一致；合并前推荐创建 `codex/web-portals-m9` 或沿用该 `codex/` 分支承载 PR。
  - `DOC/部署/m9-delivery-checklist.md` 已补充 M9 第二批 Git 策略、建议提交边界、提交命令草案和合并前最终确认项。
  - 推荐采用 PR 合并到云端主分支，不建议直接在 `main` 上提交并 push，除非明确确认主分支策略允许直接推送。
  - 本批不执行 `git add / commit / push`，等待用户确认“PR 分支”或“直接 main”策略后再做实际 Git 交付动作。
- M9 第二批验证：
  - `python3 -m unittest tests.test_webapi` 通过，23 tests OK。
  - `python3 -m compileall -q src tests` 通过。
  - `python3 -m unittest discover -s tests` 通过，141 tests OK。
  - `npm run build` 通过。
  - `npm run e2e:permissions` 通过，包含钱包/设置入口、开通广告主端、管理员等级、代看、撤销权限和审计筛选。
  - `CHABO_H5_SMOKE_AUTH_BYPASS=1 npm run smoke:h5` 通过，五个 H5 检查点无横向溢出。
  - `python3 -m chabo.cli verify-audit-chain --created-from "2026-05-02 11:00:00" --strict` 通过。
  - `python3 -m chabo.cli verify-web --profile local --skip-tests --skip-health --audit-chain` 通过。
  - `python3 -m chabo.cli verify-web --profile production --host chabo.example --health-url https://chabo.example/api/health --audit-chain --dry-run` 通过。
  - `bash -n scripts/deploy-web-production.sh scripts/rollback-web-production.sh scripts/export-audit-integrity-report.sh scripts/rehearse-sqlite-restore.sh` 通过。
  - `git diff --check` 通过；本地验证 API 已关闭。
