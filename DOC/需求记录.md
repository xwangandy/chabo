# 插播需求记录

本文用于保留用户提出过的原始需求、想法、规则和变更背景。它是“需求留痕”文档，不等同于当前执行准绳。

执行规则：

- 每次用户提出新的产品/业务/技术需求，都要先追加记录到本文。
- 需求可以过期、废弃或被施工图覆盖，但不能直接消失。
- 若需求被采纳为当前执行标准，应同步更新 `DOC/插播_项目施工图.md`。
- 若需求暂不实现，应记录到施工图待办或在本文标记为“暂缓”。
- 当本文与施工图冲突时，以施工图为准；但冲突原因应在后续整理时补充说明。

## 2026-04-26 12:16 PST - 需求留痕规则

用户要求：之后每次提出新的需求时，都必须有记录，避免后期找不到或遗忘。可以开设一个文档，把原始需求记录下来。需求可能会过期或过时，但仍然要留痕。记录后继续按项目施工路线图推进。

处理结果：

- 新增本文 `DOC/需求记录.md`。
- 将该规则同步进 `DOC/插播_项目施工图.md` 的文档地位和变更流程。
- 后续新需求先记录，再判断是否进入施工图、代码或待办。

## 2026-04-26 12:23 PST - 继续按施工图推进

用户要求：继续按照施工路线图工作，确保项目可行、能落地。

处理计划：

- 继续按 `DOC/插播_项目施工图.md` 的待办优先级推进。
- 当前优先级为广告主高级服务：优质频道发现、新频道提醒、批量投放、报表。
- 实现后同步更新施工图、README 和测试。

处理结果：

- 已实现广告主高级服务骨架：优质频道发现、频道收藏、新频道提醒规则/事件、批量投放、投放报表。
- 已实现频道主高级订阅从余额扣款购买，并将订阅收入写入平台账本。
- 已同步更新施工图、README 和测试。

## 2026-04-26 12:33 PST - 进行下一步

用户要求：继续进行下一步。

处理计划：

- 按施工图待办推进“广告主高级服务商业化：套餐价格、权限边界、到期限制”。
- 为广告主高级功能加入订阅套餐、购买/到期状态和功能权限校验。
- 同步更新施工图、README 和测试。

处理结果：

- 已实现广告主高级服务套餐：Pro $19/月、Enterprise $99/月。
- 已实现广告主余额扣款购买套餐、平台收入入账、订阅状态查询。
- 已实现到期自动失权：过期后回到免费层。
- 已实现权限边界：免费层频道发现限额；新频道提醒、批量投放、完整报表需要高级套餐。
- 已同步更新 `DOC/插播_项目施工图.md`、README 和测试。

## 2026-04-26 12:49 PST - 继续推进 Stars 支付

用户要求：继续。

处理计划：

- 按施工图待办推进 “Stars 支付 invoice 发起与订阅购买回调”。
- 增加 Stars 支付意图记录，避免只靠 payload 字符串直接入账。
- 实现 Stars 发票发送、pre_checkout_query 应答、successful_payment 幂等履约。
- 支持广告主余额充值、频道主高级订阅、广告主高级服务套餐三类 Stars 支付入口。
- 同步更新施工图、README 和测试。

处理结果：

- 已新增 `stars_payment_intents` 支付意图，记录 buyer、payload、Stars 金额、内部折算金额、支付类型和履约状态。
- 已实现 Stars 发票发送入口：余额充值、频道主高级订阅、广告主高级套餐。
- 已实现 `pre_checkout_query` 校验与 `answerPreCheckoutQuery` 应答。
- 已实现 `successful_payment` 幂等履约：余额充值、频道订阅开通、广告主套餐开通均会写入账本。
- 已同步更新 `DOC/插播_项目施工图.md`、README 和测试。

## 2026-04-26 13:00 PST - 使用真实测试 Bot 做 Telegram 环境测试

用户要求：提供一个测试 Bot token，用于导入真实环境；本机 Telegram 已登录“露出勤”账号。要求使用真实 Telegram 环境尝试并测试已有功能。

处理计划：

- 不把测试 token 写入项目文件，只在当前命令环境中临时使用。
- 先用 Bot API 验证 token 与 Bot 基本信息。
- 使用 Telegram 桌面端让已登录账号启动测试 Bot，建立真实 chat/update。
- 用真实 Bot API 或 CLI 验证消息、deep link、Stars invoice 等现有功能能否发出或收到。
- 将测试结果和阻塞点回写本记录。

处理结果：

- 已用 Bot API 验证测试 Bot 可用，Bot 用户名为 `@ChaBoADBot`，显示名为“插播广告”。
- 已删除/确认无 webhook，改用 `getUpdates` 做本地真实环境测试。
- 已用本机 Telegram 登录账号向 Bot 发送真实 `/start`，本地 `UpdateHandler` 成功处理并通过真实 Bot 回消息。
- 已绑定一个本地测试频道记录，并用真实 `/start <channel_token>` 测试 deep link 来源归因，Bot 成功返回频道广告位列表。
- 已发送 1 Star 的真实 Stars 测试发票到 Bot 对话中，只验证发票可发出，未点击付款、未完成任何资金交易。
- 当前阻塞：Python `urllib` 在本机遇到证书链校验问题；真实 Bot API 测试使用系统 `curl` 网关完成。后续真实 webhook/运行器需要处理本机 Python CA 或改用可配置 HTTP 客户端。

## 2026-04-26 13:07 PST - 真实 Bot 测试后的体验反馈

用户反馈：当前真实 Bot 测试看起来只弹出欢迎页、频道广告位和发票，整体还很简陋，不像完整产品。要求确认按照计划还有哪些需要纠正、哪些地方需要完善。

处理计划：

- 区分“链路 bug”和“产品体验未完成”。
- 按施工图重新梳理下一批必须补齐的 Bot 对话流程、真实运行器、频道接入和运营后台能力。
- 后续实现时先补真实 webhook/polling 运行器，再补 Bot 菜单化对话体验。

## 2026-04-26 13:12 PST - 按安妮建议继续补 Bot 产品外壳

用户要求：按“安妮的建议”继续推进。

处理计划：

- 先实现 `run-polling` 常驻运行器，让测试 Bot 不再依赖手工 `getUpdates`。
- 增强 Telegram 网关，支持 inline keyboard、callback query 应答、获取 update 和删除 webhook。
- 给 `/start` 和频道 deep link 增加菜单化按钮，让广告主/频道主有明确入口。
- 保留后续广告主自助下单和频道主接入向导作为下一轮重点。

处理结果：

- 已新增 `run-polling` CLI，可持续或单轮拉取真实 Telegram update。
- 已增强 Telegram 网关：支持 `getUpdates`、`deleteWebhook`、inline keyboard、`answerCallbackQuery`，并在本机 Python 证书异常时自动退回系统 `curl`。
- 已新增 polling offset 持久化，避免重启后重复处理旧 update。
- 已给 `/start`、`/menu` 和频道 deep link 增加菜单按钮：广告主入口、频道主入口、创建插播订单、查看价格、查看余额等。
- 已真实发送 `/menu` 并用 polling runner 处理，Bot 成功返回按钮菜单。
- 下一轮重点应进入广告主自助下单表单和频道主接入向导。

## 2026-04-26 14:27 PST - 继续下一步：广告主自助下单表单

用户要求：继续下一步。

处理计划：

- 实现 Bot 内广告主自助下单表单。
- 从频道 deep link 的“创建插播订单”按钮进入，选择广告形态。
- 用持久化会话状态记录文案、目标链接、预算等输入。
- 最后调用现有订单服务创建 `pending_review` 订单并冻结预算。
- 同步更新施工图、README 和测试。

处理结果：

- 已新增 Bot 会话状态表，用于持久化广告主自助下单流程。
- 已实现频道 deep link 后的“创建插播订单”按钮：广告主可选择广告形态、发送文案、发送目标链接、设置预算。
- 已接入现有订单服务，提交后创建 `pending_review` 订单并冻结广告主插播预算。
- 已支持 `/cancel` 和“取消创建”按钮，避免用户卡在未完成表单。
- 已补充自助下单测试，并同步更新施工图和 README。

## 2026-04-26 17:01 PST - 继续下一步：频道主接入向导

用户要求：询问当前进度并要求继续推进。

处理计划：

- 按施工图下一优先级实现 Bot 内频道主接入向导。
- 频道主点击“如何接入频道”后进入会话流程，提示先把插播 Bot 加为频道管理员，再转发频道消息给 Bot。
- Bot 从转发消息识别频道，检测频道主和 Bot 的频道权限。
- 权限通过后自动绑定频道、生成插播入口，并展示默认报价和广告形态配置入口。
- 增加频道主广告形态开关按钮，支持在 Bot 内启用/禁用轻插播、标准插播、强插播等形态。
- 同步更新施工图、README 和测试。

处理结果：

- 已实现 Bot 内频道主接入向导：点击“如何接入频道”后进入会话流程。
- 已支持从频道转发消息识别频道，兼容 `forward_origin`、`forward_from_chat` 和 `sender_chat`。
- 已通过 Telegram 网关能力检查频道主是否为管理员，以及 Bot 是否具备管理员、发消息、编辑消息、置顶权限。
- 权限通过后自动绑定频道、生成插播 deep link、展示默认广告位价格。
- 已新增频道主广告形态配置入口，可在 Bot 内开启/关闭轻插播、标准插播、强插播、置顶和循环插播等形态。
- 已同步更新施工图、README 和测试。

## 2026-04-26 17:07 PST - 继续下一步：运营 Admin 最小闭环

用户要求：继续按施工路线图推进。

处理计划：

- 补齐一期最小运营 Admin 能力，让自助下单产生的 `pending_review` 订单可以被审核、拒绝和退款处理。
- 增加订单拒绝并释放预算的服务与 CLI。
- 增加已扣费投放的退款服务与 CLI，写入账本并回滚广告主花费、频道主待确认收益和平台服务费。
- 增加争议列表和争议裁决 CLI，保留人工裁决记录。
- 同步更新施工图、README 和测试。

处理结果：

- 已新增 `reject-order`，可审核拒绝订单、释放冻结预算、标记素材拒绝并写入证据快照。
- 已新增 `refund-delivery`，可对已扣费投放执行全额退款，回滚广告主花费、频道主待确认收益和平台服务费，并写入账本流水。
- 已新增 `list-disputes` 和 `resolve-dispute`，支持运营查看 open 争议并写入人工裁决结果。
- 已补充订单拒绝、投放退款、争议列表和裁决测试。
- 已同步更新施工图和 README。

## 2026-04-26 17:40 PST - 真实 Bot 验收与体验要求

用户反馈：当前 Telegram 私聊页面看起来仍然简陋。开发完成后需要对每一个功能进行测试。用户已在隔壁浏览器登录 Telegram 账号，可通过私聊插播测试 Bot 进行真实功能测试，也可以由用户配合对接。

处理计划：

- 将“单元测试 + 真实 Bot 对话验收”作为后续功能完成标准。
- 每次完成 Bot 交互功能后，至少验证菜单、按钮、状态流转和错误提示。
- 对真实 Telegram 中能测试的功能，用浏览器账号或 Bot API/polling 跑一遍；涉及付款、频道权限、真实发频道消息等高风险动作，只做非资金或经用户确认后的测试。
- 本轮先检查 `/menu`、广告主入口、频道主入口、已有按钮文案，并记录/修复体验过于简陋的问题。

处理结果：

- 已优化真实 Bot 主菜单、广告主中心、频道主中心、频道 deep link 页、余额页和定价说明文案，减少“开发调试感”。
- 已用 Telegram Web 真实私聊测试 `/menu`，新版“插播控制台”菜单可正常返回。
- 已用真实 Bot 测试广告主菜单、余额页、频道 deep link、自助下单表单、订单创建和“我的订单”列表。
- 已在本地真实测试库绑定“插播真实验收频道”，给测试账号入账 USD 30，创建了一笔 USD 12 的 `pending_review` 自助订单，验证余额变为可用 USD 18 / 冻结 USD 12。
- 已用真实 Bot 测试频道主菜单、定价说明、接入向导和“非频道转发消息”的错误提示。
- 真实测试发现 polling 长轮询会因为客户端超时等于 Telegram timeout 而退出，已修复为按 API timeout 自动延长 HTTP 客户端 timeout。
- 真实测试发现频道主接入向导复用了“取消创建”按钮，已修复为“取消接入”，并新增通用取消回调。
- 测试结束后已清理测试账号当前会话状态，并停止本地 polling 进程。

## 2026-04-26 18:57 PST - 真实频道端到端联测

用户要求：已创建真实测试频道 `Squad-M4本地联测设备-A`，并已把插播 Bot 添加为管理员。要求用该频道作为测试频道，真实操作一遍；用户提供简单消息 `1` 用于测试广告/消息流程。测试完成后要对整个流程做综合测试，发现问题直接修复，直到核心流程和需要实现的功能都测试一遍，再给出下一步行动建议。

处理计划：

- 使用真实频道 `-1003925512540` 做联测，但不把测试 Bot token 写入项目文件。
- 绑定频道并检查 Bot 在频道里的管理员权限。
- 启动真实 polling，验证频道新帖自动追加“在本频道插播广告”按钮。
- 通过频道按钮/deep link 测试广告主下单、审核、调度发布、扣费、收益、争议/退款等核心闭环。
- 涉及向真实频道发消息或发布广告时，在操作前做明确确认；涉及 Stars 付款不点击支付。
- 测试发现的问题直接修复，并同步更新测试、README 或施工图。

处理结果：

- 已绑定真实测试频道 `Squad-M4本地联测设备-A`（Telegram chat id `-1003925512540`），并确认 Bot 是管理员，具备发消息和编辑消息权限；置顶权限未返回开启，因此置顶广告暂未做真实发布测试。
- 已真实向频道发布测试消息 `1`，Bot 成功收到 `channel_post` 并自动追加“在本频道插播广告”按钮。
- 已点击频道按钮进入 Bot，验证 deep link 能正确归因到该频道，并展示该频道广告位价格。
- 已测试广告主自助下单：选择 `standard_card`、提交文案、提交目标链接、设置 USD 12 预算，订单 `ord_e4b3871aac6f4de487279790cd2a22cc` 创建成功并冻结预算。
- 已验证素材规则：单字广告文案 `1` 会被拒绝并提示至少 4 个字；随后用“测试广告 1”完成订单闭环。
- 已审核订单并真实调度发布广告到频道，频道中出现广告“测试广告 1”及“查看详情”按钮，delivery message id 为 `5`。
- 已点击广告“查看详情”，Bot 成功返回广告详情链接，并写入 `bot_start` 指标。
- 已验证发布成功扣费：订单扣费 USD 10，剩余 USD 2 自动释放，低预算提醒触发；随后通过退款流程回滚。
- 已开争议、列出争议、执行退款，订单变为 `refunded`，投放变为 `refunded`，广告主花费、频道主待确认收益、平台服务费均回滚，争议自动关闭。
- 已拒绝并释放上一轮遗留测试订单，测试库最终无冻结余额残留。
- 真实联测发现同一 Telegram 用户同时作为广告主和频道主时账户 `role` 仍为单一角色，已修复为自动合并成 `mixed`。
- 真实联测发现争议退款后争议仍可能保持 open，已修复为退款时自动将 open 争议裁决为已退款。
- 真实联测再次验证 polling 长轮询会因网络超时退出的问题，已修复为 polling 捕获 `getUpdates` 网络错误并继续运行。
- 已停止本地真实 polling 进程。

## 2026-04-26 20:42 PST - 下一步行动：可运行 HTTP Webhook 与运营后台

用户要求：继续下一步行动，交付必须是可运行的，并且需要经过手动测试。

处理计划：

- 将下一步收敛为“真实可部署入口”：补 HTTP webhook 服务，避免只能靠 CLI/polling。
- 保持一期轻量，不引入额外 Web 框架，先用标准库提供可运行服务。
- 增加健康检查、Telegram webhook 入口、后台鉴权、订单审核、调度、退款、争议裁决等运营动作。
- 增加 Telegram `setWebhook` 辅助命令，支持 webhook secret。
- 完成自动化测试后，用本地 HTTP 服务和 `curl` 手动验收。

处理结果：

- 已新增 `chabo run-web`，启动 `/health`、`/telegram/webhook/<secret>` 和 `/admin`。
- 已新增后台 token 鉴权，支持 query token、`Authorization: Bearer` 和 `X-Chabo-Admin-Token`。
- 已新增后台订单列表、投放列表、争议列表，以及审核通过、审核拒绝、调度、收益确认、投放退款、争议解决动作。
- 已新增 `chabo set-webhook`，可将 Telegram webhook 指向公网 HTTPS 地址，并配置 `secret_token`。
- 已补充 `.env.example`、README 和施工图。
- 已新增 HTTP webhook/admin 单元测试，并通过 31 个测试。
- 已用独立测试库 `.runtime/manual-webtest.sqlite3` 启动本地 HTTP 服务，手动验证：
  - `/health` 返回正常。
  - 未带 token 访问 `/admin` 返回 401。
  - `/admin/orders?token=...` 能列出待审核订单。
  - `POST /telegram/webhook/<secret>` 能处理频道帖 update。
  - 错误 webhook secret 返回 403。
  - `POST /admin/orders/<order_id>/approve` 能审核通过订单。
  - `/admin?token=...` 能打开运营后台 HTML 页面。

## 2026-04-26 21:28 PST - 下一步行动：运营后台增强与部分退款

用户要求：开始下一步。

处理计划：

- 优先做“运营 Admin 增强”中最影响真实运营判断的部分。
- 增加订单、投放、争议详情页，让运营能看到证据链、账本流水和关联对象。
- 增加部分退款能力，支持运营按金额退还广告主，并按比例回滚频道主待确认收益和平台服务费。
- 部分退款必须避免后续收益确认重复确认已回滚部分。
- 更新 CLI、HTTP 后台、测试、README 和施工图。

处理结果：

- 已为 `deliveries` 增加 `refunded_cents`、`publisher_reversed_cents`、`platform_fee_reversed_cents`，记录投放退款累计值。
- 已新增部分退款服务 `refund_delivery_partial`，CLI 支持 `refund-delivery --amount`。
- HTTP 后台退款表单支持填写 `amount`；不填时仍为全额/剩余可退金额退款。
- 已新增订单、投放、争议详情页，展示关联对象、证据链和账本流水；详情页同时支持 JSON 与 HTML。
- 收益确认已改为只确认 `publisher_net_cents - publisher_reversed_cents`，避免部分退款后重复确认已回滚收益。
- 已更新 README 和施工图。
- 已新增部分退款与 HTTP 后台详情测试，并通过 32 个测试。
- 已用独立测试库 `.runtime/manual-admin-enhanced.sqlite3` 启动手动 HTTP 服务，验证：
  - `/admin/orders/<order_id>` 能返回订单、投放、证据链和账本流水。
  - `/admin/deliveries/<delivery_id>` HTML 详情页能打开。
  - `POST /admin/deliveries/<delivery_id>/refund` 带 `amount=4` 后，投放保持 `sent`，订单已花从 USD 10 降为 USD 6，退款累计为 USD 4。
  - 同一投放再次不带 `amount` 退款剩余金额后，订单和投放变为 `refunded`，退款累计为 USD 10，订单已花归零。

## 2026-04-26 22:05 PST - 真实 Bot 测试入口偏好

用户建议：真实 Bot 交互测试应优先通过当前 Telegram Web 私聊输入框进行，而不是只通过本地 HTTP/curl 或 Bot API。

处理结果：

- 已把“通过 Telegram Web 输入框做真实体验测试”记录为后续验收偏好。
- 已启动真实 polling，并通过 Telegram Web 私聊输入 `/menu`，Bot 成功返回“插播控制台”。
- 已点击“我是广告主”，Bot 成功返回广告主中心和余额信息。
- 已点击“我的订单”，Bot 成功返回最近插播订单列表，能看到真实联测订单状态。
- 测试完成后已停止本地真实 polling 进程。

## 2026-04-26 22:43 PST - Bot 产品界面与交互体验优化

用户反馈：当前 Bot 界面像说明书，文案过长、按钮缺少图标、满屏都是字；按钮点击后有时过一会儿集中弹出，造成刷屏和迟钝感。希望增加 emoji/图标提示，压缩文案，让点击后界面更清爽。

处理计划：

- 主菜单、广告主中心、频道主中心、余额、订单、价格、接入、广告形态等关键页面增加 emoji。
- 文案压缩，减少说明书式长段落。
- callback 按钮处理时返回“处理中...”反馈，减少无响应感。
- 对 callback 产生的页面优先使用 `editMessageText` 原地更新当前 Bot 消息；失败时再 fallback 为发送新消息，减少刷屏。
- 通过 Telegram Web 私聊输入框进行真实体验验收。

处理结果：

- 已将主菜单、广告主中心、频道主中心、余额、订单、价格、频道报价、自助下单和广告形态配置等核心页面改为短文案 + emoji 引导。
- 已新增 Bot API `editMessageText` 网关能力；用户点击 inline 按钮后，优先原地更新当前 Bot 消息，不再默认连续发送新消息。
- 已在 callback query 应答中加入“处理中...”即时反馈，降低按钮点击后无反应的体感。
- 已补充 FakeGateway 与自动化测试断言，确保新版按钮文案和频道 deep link 页面不回退到旧文案。
- 已通过 Telegram Web 私聊真实验收：发送 `/menu` 后显示“📌 插播控制台”；点击“📣 我是广告主”后同一条消息更新为“📣 广告主中心”；点击“💰 余额”后同一条消息更新为余额页。
- 已运行 `python3 -m compileall -q src tests` 和 `python3 -m unittest discover -s tests`，32 个测试全部通过。

## 2026-04-26 23:20 PST - 启动真实测试 Bot 供用户联测

用户要求：启动程序，现在要进行测试，继续使用之前的测试 Bot。

处理结果：

- 已重启真实 polling 进程，使用 `.runtime/chabo-realtest.sqlite3` 接收 Telegram 真实消息。
- 已启动本地 HTTP 服务 `http://127.0.0.1:8080`，后台 token 仍使用本地测试配置。
- 已确认真实 Bot 私聊可响应新版 `/menu`、广告主中心和余额页。

## 2026-04-26 23:35 PST - 频道主资产识别与频道列表

用户反馈：频道主已经把插播 Bot 加入测试频道并设为管理员，但私聊 `/start` 后 Bot 仍像不认识这个频道主一样，要求重新接入，这不合理。频道主入口应能识别“当前用户和 Bot 是否同为某频道管理员”，并列出可管理频道资产；当 Bot 被加入频道并成为管理员时，应检测频道管理员，尽可能通知已能私聊的管理员，提示频道资产已添加并可继续配置。希望参考 Channel Helper / GroupChannel 类机器人的频道资产管理交互。

处理计划：

- 增加 Telegram `my_chat_member`/`chat_member` update 支持，用于识别 Bot 被加入频道、升为管理员、被降权或移除。
- 对已知频道建立频道资产库，并同步频道管理员关系；Bot 无法通过 Bot API 枚举用户所有频道，只能识别 Bot 已经接入或收到事件的频道。
- `/start` 和频道主中心自动查询当前用户可管理的已知频道，若有频道则展示“我的频道”列表和配置入口。
- 当 Bot 成为频道管理员时，绑定/更新频道，检测管理员列表，并给已能私聊 Bot 的管理员发送“频道已接入”提醒；无法私聊的管理员静默跳过。
- 将真实测试频道 `Squad-M4本地联测设备-A` 纳入验收，确认当前账号可在频道主中心看到该频道。

处理结果：

- 已新增 `channel_admins` 表，记录频道管理员、Bot 权限、展示名和最近同步时间。
- 已支持 `my_chat_member` 和 `chat_member` update；polling/webhook 的 `allowed_updates` 已包含这两类事件。
- Bot 被加入频道或升为频道管理员时，会自动绑定/更新频道资产，同步 `getChatAdministrators` 返回的管理员列表，并尝试私聊通知已可触达的管理员。
- `/start` 无参数时会检查当前用户是否管理已知频道；如果是频道主，会直接进入“📺 频道主中心”并列出“我的频道”。
- 频道主中心已增加频道资产列表，点击频道后进入频道仪表盘，可查看入口链接、价格、管理员识别数量、权限检查，并进入广告形态配置。
- 频道广告形态配置和开关已增加管理员校验，避免非管理员拿到 token 后直接改频道配置。
- 已新增自动化测试覆盖：频道主 `/start` 自动识别资产、Bot 被加为管理员时自动绑定频道/同步管理员/发送通知。
- 已运行 `python3 -m unittest discover -s tests`，34 个测试全部通过。

## 2026-04-27 10:55 PST - 首次时区引导与双身份首页

用户反馈：参考 Channel Help，用户第一次 `/start` 时应先确认时区，默认北京时间/上海时区；如果不是，则让用户输入城市或时区并保存。后续涉及“晚上 8 点发广告”等时间设置时，必须明确使用用户时区。用户还明确要求：不要再问“你是广告主还是频道主”，默认每个人同时具备广告主和频道主两种身份，只是是否启用而已。首页应直接提供可操作入口：顶部“添加频道”，左侧偏频道主管理，右侧偏广告主管理；频道管理进入后列出共同管理员频道；还要有“我的收益”和“广告钱包/账户余额”两个钱包视角。

处理计划：

- 给账号增加 `timezone` 与 `timezone_confirmed_at`，首次 `/start` 前先做时区确认。
- 默认时区为 `Asia/Shanghai`，按钮确认后保存；选择重选后进入输入流程，支持常见城市/国家关键词和 IANA 时区名。
- 首页改为双身份控制台，不再出现“我是广告主/我是频道主”的二选一问法。
- 顶部增加“➕ 添加频道” URL 按钮，使用 Telegram 原生 `startchannel` deep link 引导用户把 Bot 添加为频道管理员。
- 首页左侧提供“频道管理/我的收益”，右侧提供“我的广告/广告钱包”。
- 频道管理页继续列出当前用户和 Bot 共同管理的频道资产。

处理结果：

- 已给 `accounts` 增加 `timezone` 与 `timezone_confirmed_at` 字段，默认时区为 `Asia/Shanghai`。
- 首次 `/start` 或 `/menu` 时，如果账号未确认时区，会先显示“🌐 时区确认”；点击“✅ 是的”保存北京时间，点击“🌐 重选”进入城市/时区输入。
- 已支持常见城市/地区关键词和 IANA 时区名，例如北京、上海、Manila、Asia/Tokyo、Europe/Rome。
- 首页已改为双身份控制台，不再要求用户选择“我是广告主/我是频道主”。
- 首页顶部新增“➕ 添加频道” URL 按钮，使用 Telegram `startchannel` deep link 引导添加 Bot 到频道并申请发消息、编辑消息、置顶权限。
- 首页双栏入口已更新为“📺 频道管理 / 📣 我的广告”和“💸 我的收益 / 💰 广告钱包”。
- 已新增“我的收益”页面，展示待确认、已确认、可结算收益；广告侧余额页改名为“广告钱包”。
- 已新增自动化测试覆盖时区确认、城市输入、双身份首页入口；`python3 -m unittest discover -s tests` 已通过 35 个测试。
- 已重启真实测试 Bot，并在 Telegram Web 私聊中手动验收：`/start` 显示“🌐 时区确认”，点击“✅ 是的”后显示新版“📌 插播控制台”，按钮包含“➕ 添加频道 / 📺 频道管理 / 📣 我的广告 / 💸 我的收益 / 💰 广告钱包”；点击“📺 频道管理”可看到已识别频道资产。

## 2026-04-28 08:45 PST - 对标 Channel Help 优化菜单体验

用户反馈：当前插播 Bot 仍然显得简陋，像一本说明书，按钮排布和视觉层级不够好。要求参考 Telegram 上的 Channel Help Bot，随便点开看看它的交互方式，再对比插播自己的界面。用户特别强调按钮应根据字数和重要程度排布：重要动作可以独占一行，次级动作应两列或多列并排；按钮前面要有合适 emoji，正文尽量短，不要满屏说明文字。当前感觉“不对劲”，需要继续调整。

处理计划：

- 将 Bot 私聊菜单定义为“插播工作台”，不做长说明书。
- 顶部主动作使用整行按钮，例如“➕ 添加频道”。
- 频道侧和广告侧入口左右并排，形成稳定的双栏心智。
- 频道列表、广告中心、钱包、收益、频道详情页都使用短标题、状态摘要和分组按钮。
- 广告形态、价格等技术字段面向用户时必须转成中文产品名，避免展示 `standard_card`、`strong_post` 这类内部字段。

处理结果：

- 已对照 Channel Help 的主菜单结构，将插播首页改为“📌 插播工作台”，正文压缩为短句。
- 已保留“➕ 添加频道”为顶部整行主按钮；频道侧/广告侧入口、收益/钱包入口、定价/时区入口均改为双列并排。
- 已优化频道管理页：频道资产列表保持单频道整行，辅助动作改为两列分组。
- 已优化频道详情页：用“状态、权限、管理员、当前价、入口链接”的摘要结构替代长说明。
- 已将广告位展示名从内部字段改为中文产品名，例如“轻插播、标准插播、强插播、置顶 24h、循环插播”。
- 已补充自动化测试，防止首页回退到“我是广告主/我是频道主”二选一，并防止广告形态页暴露 `strong_post` 等内部字段。
- 已运行 `python3 -m compileall -q src tests` 和 `python3 -m unittest discover -s tests`，35 个测试通过。
- 已重启真实测试 Bot，并在 Telegram Web 手动验收：`/menu` 展示新版“📌 插播工作台”；“频道管理”展示频道资产与双列辅助动作；频道详情展示状态、权限、当前价；“广告形态”展示中文广告形态和双列开关按钮。

## 2026-04-28 09:20 PST - Codex 多窗口总控工作流

用户要求：当前项目运行在本地 MacBook，用户与主 Codex 窗口在 Codex 桌面端聊天。希望建立一套工作流：主窗口作为产品总监/总工程师/总控，负责理解需求、查资料、制定规则、拆分任务、验收测试和合并；其他终端版或隔壁窗口 Codex 作为执行窗口，按主窗口给出的任务包在独立 worktree 中开发。执行窗口只负责具体实现，完成后由主窗口 review、测试、发现问题、返工或合并。希望形成闭环，并维护一份最终组文档，保证项目高效推进、可落地。

处理计划：

- 新增一份 `DOC/Codex_多窗口协作工作流.md` 作为总控协作准绳。
- 明确主窗口、执行窗口、用户三个角色的边界。
- 使用 git branch + git worktree 作为并行开发隔离机制。
- 定义任务包格式、执行窗口完成报告格式、主窗口验收清单、合并规则和冲突处理规则。
- 记录当前前提：本目录当前尚未初始化 git 仓库，进入 worktree 流程前需要先做一次版本化基线。

处理结果：

- 已新增 `DOC/Codex_多窗口协作工作流.md`，明确主窗口、执行窗口和用户的职责边界。
- 已新增 `.gitignore`，防止 `.runtime/`、sqlite 数据库、日志、`.env`、缓存和系统文件进入版本库。
- 用户已创建 GitHub 仓库 `https://github.com/xwangandy/chabo`，并指定它作为后续协作云端仓库。
- 已初始化本地 git 仓库，主分支为 `main`，远程为 `origin`，并推送 baseline commit。
- 后续给其他 Codex 窗口派任务时，统一基于 `origin/main` 创建独立 branch + worktree。

## 2026-04-28 10:35 PST - 频道招商金线与来源入口

用户要求：现在要重新梳理项目进展、本地 main 主项目、云端仓库、交互逻辑和后续缺口。用户强调主 Codex 窗口应主动发现逻辑不合理的地方，不能只等用户逐条指出。产品风格可以参考 Channel Help，但插播不能只像说明书。所有用户默认同时可能是广告主和频道主，首页不应明显二选一。

用户进一步明确：插播最核心的金线任务，是频道主把 Bot 加入频道后，频道里的广告/帖子带来新广告主。广告主大概率不是从首页主动探索来的，而是从某个频道里的入口点进来。因此用户点击“频道招商”后，Bot 必须知道他来自哪个频道，并第一时间展示“向这个频道投广告”的流程，而不是先宣传机器人或让用户进入通用菜单。

关键入口设想：

- 轻插播：一句短文案或短按钮，点击后进入 Bot 展示完整广告详情，同时记录真实兴趣信号。
- 标准插播：固定模板广告，带固定按钮。第一行左侧是“频道招商”，第一行右侧是“查看详情”，第二行整行是广告主主 CTA，例如“点击购买/点击开始/点击游玩”。
- “频道招商”带来源频道参数，进入 Bot 后直接展示该频道报价、可投广告形态、广告库、广告钱包和“投广告到该频道”的主动作。
- “查看详情”带素材/投放参数，进入 Bot 后展示广告完整内容和详情，记录兴趣信号。
- 广告主主 CTA 属于广告主，跳转到广告主目标链接或目标 Bot。

用户要求广告主侧必须有“广告库”：广告主能维护轻插播、标准插播、强插播三类广告素材，然后把素材投放到指定频道。广告钱包后续也要更完整，至少展示余额、充值入口、每频道预算、冻结预算和预算不足引导。

用户还要求频道主可以把插播作为自己的频道工具使用：种子频道主可以用 Bot 发布自己的广告、运营内容或自动化插播；这些自用内容默认也可以带“频道招商/查看详情”等插播增长入口。平台可以不向频道主自己的内容收费，但要借此形成第一批广告主来源。

处理计划：

- 更新 `DOC/插播_项目施工图.md`，把“频道招商金线任务”写成当前施工准绳。
- 将当前缺口调整为：频道招商 payload 和首屏、标准插播三按钮、查看详情完整页、广告库、频道主自用发布工具、广告钱包增强。
- 后续实现时优先做“频道广告里的按钮 -> Bot -> 投广告到该频道”的最短路径。

处理结果：

- 已把本需求留痕。
- 已同步更新施工图中的标准插播定义、平台推广按钮规则、频道招商金线任务和待办优先级。
- 已同步更新多窗口协作文档，把“当前建议第一步”从 git 初始化改为 `CHB-001` 到 `CHB-005` 的频道招商金线任务队列。
- 已完成 `CHB-001` 初版：频道帖子自动追加按钮改为“频道招商”，频道 deep link 改为 `ch_<ref_token>`，用户从频道招商进入后第一屏直接展示来源频道、当前价、“投广告到该频道”、广告库、广告钱包和价格说明。
- 已新增广告库入口的最小页面：可展示广告主已有素材；没有素材时引导从频道招商创建第一条插播。
- 已运行 `python3 -m compileall -q src tests` 和 `python3 -m unittest discover -s tests`，35 个测试通过。
- 已用真实测试 Bot 和真实测试频道验证：模拟 `/start ch_<频道token>` 返回 `channel_start`；向 `Squad-M4本地联测设备-A` 发送测试消息后，Bot 成功给该消息追加“频道招商”按钮。

## 2026-04-28 11:15 PST - 轻插播说明与开发期自动审核

用户反馈：选择“轻插播”后，Bot 只提示“请直接发送广告文案”，太不清楚。轻插播应该解释清楚：它是在频道最新发布的帖子下方/底部增加一行 15 字以内的广告短入口，用户点击后再跳转到 Bot 展示完整广告详情。轻插播下单时不能只收短文案，还必须收完整广告内容，因为完整广告是在点击短入口后展示的。

用户还反馈：订单创建后显示“等待审核”，但当前测试阶段还没有清晰审核流程。正式审核应该把内容发给管理员，由管理员确认是否通过；但现在为了先跑通整体闭环，可以暂时关闭审核或让自助订单自动通过，等上线前再完善审核环节。

处理计划：

- 轻插播下单流程拆成“短入口”和“完整广告详情”两步。
- 短入口限制 15 个字以内，完整详情用于 Bot 内广告详情页展示。
- 点击广告 deep link 时，不再只展示目标链接，而是展示完整广告详情和打开链接按钮。
- 增加开发期 Bot 自助订单自动审核通过配置，默认用于本地联测；正式运营前关闭或改为管理员审核。

处理结果：

- 已完成轻插播下单流程改造：选择轻插播后先解释展示效果，再要求输入 15 字以内短入口，随后收完整广告详情和目标链接。
- 已让轻插播投放在当前 MVP 中以一行短入口形式发送，并用“查看完整广告”按钮跳转 Bot 详情页。
- 已把 `ad_<delivery_id>` 详情页从只显示目标链接改为显示完整广告详情和打开链接按钮。
- 已新增 `CHABO_BOT_AUTO_APPROVE_ORDERS` 配置；默认开发联测自动通过 Bot 自助订单，设置为 `0/false/no/off` 可关闭。
- 已运行 `python3 -m compileall -q src tests` 和 `python3 -m unittest discover -s tests`，36 个测试通过。
